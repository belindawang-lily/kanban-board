# 投资管理训练营运营进度看板 · 用户手册

---

## 一、系统简介

本系统用于追踪投资管理训练营 12 支队伍的 OKR 进度、双周报提交和日常打卡情况。

数据存储在飞书多维表格中，通过同步脚本生成 HTML 看板。队员在飞书中填报数据，管理员定期同步到看板。

飞书 Base 地址：`https://qcnjj22jqvr1.feishu.cn/base/NVokbNXaca3oihspnCicYyccnVe`

---

## 二、用户角色与权限

| 角色 | 看板查看 | 飞书 Base 权限 | 典型用户 |
|------|---------|---------------|---------|
| 管理员 | 全部页面 | 全部表可编辑 | 运营负责人 |
| 队长 | 全部页面 | 本团队记录可编辑，其他只读 | 各队队长 |
| 队员 | 全部页面 | 本团队记录可编辑，其他只读 | 各队成员 |
| 只读 | 全部页面 | 全部只读 | 外部观察者 |

飞书 Base 侧已配置 12 个团队角色（团队1编辑 ~ 团队12编辑），每个角色只能编辑 `所属团队 = 本团队` 的记录，但可查看所有团队的记录。

---

## 三、日常操作流程

### 3.1 队员：填报双周报

1. 打开看板 `index.html`，点击顶部导航「双周报」
2. 点击「填报新一期」按钮，跳转到飞书 Base 双周报表
3. 在飞书中填写：
   - 报告标题：双周报第N期（YYYY.MM.DD-YYYY.MM.DD）- 团队X
   - 周期：选择对应期数
   - 所属团队：下拉选择本团队
   - 记录人：填写自己的姓名
   - 本期进展：描述本期完成的工作
   - 问题与需求：列出遇到的问题和需要的支持
   - 本期更新KR：勾选本期有进展的关键结果
4. 点击飞书中的保存

### 3.2 队员：日常打卡

1. 打开看板，点击顶部导航「日常打卡」
2. 点击「新增打卡」按钮，跳转到飞书 Base 打卡表
3. 在飞书中填写：
   - 研讨内容：简要描述活动内容
   - 活动时间：选择日期
   - 所属团队：下拉选择本团队
   - 团队内参与人：勾选参与活动的本队成员
   - 其他参与人：如有非本队参与人，填写姓名
   - 打卡照片：上传活动照片
4. 点击飞书中的保存

### 3.3 队长：管理 OKR

1. 打开看板，点击顶部导航「OKR 管理」
2. 在飞书 Base 中更新关键结果：
   - KR 标题：描述关键结果
   - 所属目标：关联到对应目标
   - 状态：选择（未开始/进行中/已完成/有风险）
   - 进度：填写百分比数值（如 0.65 表示 65%）
   - 所属团队：下拉选择本团队
   - 更新时间：自动记录

### 3.4 队长：管理成员

1. 打开看板，点击顶部导航「队伍管理」
2. 点击「添加成员」按钮，跳转到飞书 Base 成员表
3. 在飞书中填写：
   - 姓名：新成员姓名
   - 所属团队：下拉选择本团队
   - 角色：选择（队长/队员）
   - 身份：选择（成员/志愿者/内部导师/外部导师）
   - OA号：如有则填写

### 3.5 管理员：同步看板数据

队员在飞书中填报数据后，看板不会自动更新。管理员需要手动运行同步脚本：

```bash
cd kanban-board
node scripts/sync-via-lark-cli.mjs
```

同步完成后，刷新浏览器（Ctrl+F5）即可看到最新数据。

**同步频率建议**：每周至少 1 次，或双周报截止日后立即同步。

---

## 四、看板页面说明

### 4.1 总看板（index.html）

系统入口页，展示全局概览：

- **核心指标**（一行 6 项）：队伍数、成员数、OKR 平均进度、本期双周报提交率、打卡活动数、有风险 KR 数
- **各队 OKR 进度总览表**：12 支队伍的进度条、KR 数、最新双周报、打卡数。点击任意队伍行，跳转到该队 OKR 详情
- **打卡活跃度 Top 6**：横向条形图，展示打卡最活跃的 6 支队伍
- **本期双周报提交情况**：各队提交状态和提交率进度条
- **身份参与统计**：按身份分类统计参与人次

### 4.2 队伍管理（team-management.html）

- 左侧队伍列表，右侧队伍详情
- 队伍详情含队长、周期、成员数、打卡数
- 成员名册展示姓名、身份、角色、OA 号、累计参与次数
- 身份分布卡（人数 + 占比）
- 导出成员名单 CSV：点击「导出名单」按钮

### 4.3 OKR 管理（okr.html）

- 默认展示全部队伍 OKR 总览
- 页面顶部有队伍下拉选择器，选择后自动跳转单队视图
- 也可直接在浏览器地址栏输入 `okr.html?team=team1` 查看 team1 的详情（team1~team12 对应团队1~团队12）
- OKR 树形展示：目标（O）→ 关键结果（KR）
- KR 进度条 + 状态标签（未开始/进行中/已完成/有风险）
- KR 状态分布统计

### 4.4 双周报（biweekly-report.html）

- 默认展示全部队伍双周报总览
- 页面顶部有队伍下拉选择器，选择后自动跳转单队视图
- 也可直接在浏览器地址栏输入 `biweekly-report.html?team=team3` 查看 team3 的详情
- 时间线展示每期报告
- 每期含报告标题、周期、记录人、本期进展、问题与需求

### 4.5 日常打卡（checkin.html）

- 默认展示全部队伍打卡概览
- 页面顶部有队伍下拉选择器，选择后自动跳转单队视图
- 也可直接在浏览器地址栏输入 `checkin.html?team=team5` 查看 team5 的详情
- 打卡时间线：活动时间、研讨内容、打卡照片、参与人头像组
- 身份参与统计柱状图
- 导出打卡统计 CSV：点击「导出统计」按钮，可选按队伍汇总或按人明细

### 4.6 项目快报（quick-brief.html）

- 下拉选择队伍，生成单队快报；或生成整体快报
- 快报内容：整体概览 → OKR 排名 → 双周报亮点 → 打卡统计 → 风险预警 → 任务完成率 → 下阶段重点
- 导出 PDF：点击「导出 PDF」按钮，使用浏览器打印功能

---

## 五、数据导出

| 导出内容 | 位置 | 格式 |
|---------|------|------|
| 成员名单 | 队伍管理页 → 导出名单 | CSV |
| 打卡统计（按队伍） | 日常打卡页 → 导出统计 | CSV |
| 打卡统计（按人明细） | 日常打卡页 → 导出统计 | CSV |
| 项目快报 | 项目快报页 → 导出 PDF | PDF |

CSV 文件带 BOM 头，可直接用 Excel 打开，中文不乱码。

---

## 六、飞书 Base 权限管理

### 6.1 查看权限配置

1. 打开飞书 Base
2. 点击右上角蓝色「分享」按钮
3. 弹出对话框中显示"高级权限已开启，用户的权限同时受到角色设定限制"
4. 点击右侧「高级权限」或关闭对话框后点击 Base 页面左下角的「高级权限」入口

### 6.2 分配成员到角色

1. 在高级权限管理界面，左侧角色列表中选择目标角色（如"团队1编辑"）
2. 添加协作者到该角色
3. 或在「分享」对话框中搜索协作者，邀请后在权限下拉框中选择角色

### 6.3 权限规则

每个团队角色（如"团队1编辑"）的权限：

- 成员、目标、关键结果、双周报、打卡 5 张表：可编辑，但仅限 `所属团队 = 团队1` 的记录
- 其他团队的记录：可查看，不可编辑
- 队伍表：只读
- 可阅读所有记录，可新增和删除本团队记录

---

## 七、常见问题

### Q1：看板数据不是最新的？

看板是静态快照，需要管理员运行同步脚本刷新。联系管理员执行 `node scripts/sync-via-lark-cli.mjs`。

### Q2：点击「填报」按钮后跳转到飞书，但看不到编辑入口？

确认你的飞书账号已被分配到对应的团队角色。联系管理员在高级权限界面分配。

### Q3：看不到其他团队的数据？

在飞书 Base 中，所有团队的记录都可查看（只读）。如果完全看不到数据，可能是飞书 Base 分享权限未设置，联系管理员检查分享设置。

### Q4：进度显示不对？

飞书中进度字段是百分比格式（0.65 = 65%）。同步脚本会自动转换为 0-100 整数。如果显示异常，检查飞书中的进度值是否在 0-1 范围内。

### Q5：双周报期数编号有误？

双周报标题格式为"双周报第N期（YYYY.MM.DD-YYYY.MM.DD）- 团队X"。期数从第1期开始递增。如果编号有误，在飞书 Base 中修改「周期」字段。

### Q6：如何离线查看看板？

看板是纯 HTML 文件，双击 `index.html` 即可在浏览器中查看，无需联网。但数据不会自动更新，需要联网运行同步脚本后才能刷新。

### Q7：如何在不同电脑上使用？

将整个 `kanban-board` 文件夹复制到目标电脑即可。同步脚本需要目标电脑安装 Node.js 18+ 和 lark-cli 并登录。
